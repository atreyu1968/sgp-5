import React from 'react';
import RegisterPage from '../RegisterPage';

const ReviewerRegisterPage: React.FC = () => {
  return <RegisterPage expectedRole="reviewer" />;
};

export default ReviewerRegisterPage;