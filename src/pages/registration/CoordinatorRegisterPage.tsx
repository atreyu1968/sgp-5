import React from 'react';
import RegisterPage from '../RegisterPage';

const CoordinatorRegisterPage: React.FC = () => {
  return <RegisterPage expectedRole="coordinator" />;
};

export default CoordinatorRegisterPage;